
## Remove the "Works Offline" Badge

One small change to `src/pages/Index.tsx`.

Delete lines 147–154 — the `<span>` element that renders the "Works Offline" badge in the header. Nothing else changes.
